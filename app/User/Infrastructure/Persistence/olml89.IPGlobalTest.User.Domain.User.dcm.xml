<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="https://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="https://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="https://doctrine-project.org/schemas/orm/doctrine-mapping
                          https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd"
>

    <entity name="olml89\IPGlobalTest\User\Domain\User" table="users">

        <indexes>
            <index
                name="idx_id"
                columns="id"
            />
        </indexes>

        <id name="id"
            column="id"
            type="uuid"
        />

        <field
            name="name"
            column="name"
            type="string"
            column-definition="VARCHAR(100) NOT NULL"
        />

        <field name="username"
               column="username"
               type="string"
               column-definition="VARCHAR(100) NOT NULL"
        />

        <field name="email"
               column="email"
               type="string"
               column-definition="VARCHAR(320) NOT NULL"
        />

        <embedded name="address"
                  class="olml89\IPGlobalTest\User\Domain\Address\Address"
        />

        <field name="phone"
               column="phone"
               type="string"
               column-definition="VARCHAR(100) NOT NULL"
        />

        <field name="website"
               column="website"
               type="string"
               column-definition="VARCHAR(2048) NOT NULL"
        />

        <embedded name="company"
                  class="olml89\IPGlobalTest\User\Domain\Company"
        />

    </entity>

</doctrine-mapping>
